# Mapbox Configuration
# No token required! Using MapLibre GL with free tile sources.

# Database Configuration (PostGIS)
# Update these values to match your PostgreSQL/PostGIS setup
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fra_atlas
DB_USER=postgres
DB_PASSWORD=your_database_password_here

# Database Mode
# Set to 'true' to use real database, 'false' to use mock data (default)
USE_DATABASE=false

# Prisma Database URL (for DSS module)
# Uncomment and configure if using Prisma with PostgreSQL
# DATABASE_URL="postgresql://postgres:your_password@localhost:5432/fra_atlas?schema=public"
